server:
  port: 9010
spring:
  application:
    name: tensquare-friend
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://39.105.97.53:3306/unicorn_friend?characterEncoding=utf-8&useSSL=false
    username: root
    password: root1234
  jpa:
    database: mysql
    show-sql: true
jwt:
  config:
    key: unicorn
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:6868/eureka/
  instance:
    prefer-ip-address: true